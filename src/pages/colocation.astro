---
import Layout from "../layouts/Layout.astro";
import ProviderGrid from "../components/ProviderGrid.astro";
import { getSortedProviders } from "../utils/providers";
const sortedProviders = await getSortedProviders(entry => 
  entry.data.services.some(service => service.toLowerCase().includes("colocation"))
);
---

<Layout 
  title="BGP-enabled Colocation Providers Directory | BGP.mom"
  description="Find and compare top BGP-enabled colocation providers. A community-curated directory of data centers offering BGP sessions for your ASN."
>
    <div class="text-center mb-8">
      <h1 class="text-2xl sm:text-4xl font-bold text-base-content mb-2">Colocation Providers</h1>
      <p class="text-base sm:text-lg text-base-content/70">Providers offering colocation services with BGP support</p>
      <p class="text-sm text-base-content/50 mt-2">Found {sortedProviders.length} providers</p>
    </div>
    
    <ProviderGrid providers={sortedProviders} />
    
    {sortedProviders.length === 0 && (
      <div class="text-center py-12">
        <p class="text-xl text-base-content/70">No colocation providers found.</p>
      </div>
    )}
</Layout>
